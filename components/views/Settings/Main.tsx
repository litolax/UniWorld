import { Title } from '@mantine/core'

export const Main = (): JSX.Element => {
  return (
    <div>
      <Title order={1} mb={'10px'}>
        Main settings page
      </Title>
    </div>
  )
}

export default Main
