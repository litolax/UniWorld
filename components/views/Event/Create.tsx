import { Title } from '@mantine/core'

export const Create = (): JSX.Element => {
  return (
    <div>
      <Title order={1} mb={'10px'}>
        Main event create page
      </Title>
    </div>
  )
}

export default Create
